# Netlify / Cloudflare Pages: serve index.html for client-side routes (React Router)
# Render static sites ignore this file — set Rewrite /* → /index.html in the Render Dashboard
# (or use routes in render.yaml at the repo root).
/*    /index.html   200
